ability to toggle the freshness indicator, or add and remove it.
also the ability to set it manually to FRESH BEHIND or OLD.

divider lines in the nodes, sometimes there are double lines, fix that. also the description field for the tools should actually be the one that is not left and right aligned but both left aligned so its easier to read the description.

 make the sidebar a little smaller

fixed seperator in the canvas where i can move node groups up or below it to seperate the ones i currently use from the other ones

render to image export option

fix the styling of the canvas overview thing in the bottom right, or just disable it?

a bug where setting the speed property to nothing after it was some number before causes the bar to be red but almost full while also saying very slow. it should just not be shown if not set at all tho.

a bug in the image export that causes the connections to not be rendered at all

all in one tools like v0, lovable and orchids, they are basically tools but as webapps

---

extract the user interaction prompt i gave to tell it to implement features at a pace which allows for better paired programming with me being the oen who tests the changes made to a website for example to then provide feedback on those changes.

also create a prompt template that tells an agent to use websearch tools to udpate a exported json file with the latest news. openrouter is also a good source for new models and their speed and context window size